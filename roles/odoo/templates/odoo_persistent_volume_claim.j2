kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "{{ _odoo_name }}-pvc"
  namespace: "{{ _odoo_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ _odoo_volume_size }}"
